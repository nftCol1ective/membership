{
  "name": "membership",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nftCol1ective/membership",
    "create-local": "graph create --node http://localhost:8020/ nftCol1ective/membership",
    "remove-local": "graph remove --node http://localhost:8020/ nftCol1ective/membership",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nftCol1ective/membership"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
